<%= form_for(:add_trip) do |f| %>
  <% if @add_trip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@add_trip.errors.count, "error") %> prohibited this add_trip from being saved:</h2>

      <ul>
      <% @add_trip.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :location %><br>
    <%= f.text_field :location %>
  </div>
  <div class="field">
    <%= f.label :destination %><br>
    <%= f.text_field :destination %>
  </div>
  <div class="field">
    <%= f.label :travelDate %><br>
    <%= f.date_select :travelDate %>
  </div>
  <div class="field">
    <%= f.label :maxWeight %><br>
    <%= f.number_field :maxWeight %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
